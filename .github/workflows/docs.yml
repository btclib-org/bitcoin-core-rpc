---
name: docs

# the third gate, and a question of its own: does the documentation build.
# Without it nothing in CI builds it, only read the docs does, with -W, so
# invalid reStructuredText in a docstring fails after the merge on a service
# whose failure is not a check on the pull request -- a name ending in an
# underscore, which rst reads as a reference to a link target, or a title
# underline left too short by a rename.
#
# The build itself, the checkout and the unresolved-link check are
# btclib-org/.github's: reusable-docs.yml, called at @main so a fix there
# reaches this tree in one landing (section 10 of its README.md). What
# stays here is the trigger, the concurrency group and the one thing this
# tree asks of its own built pages that the standard asks of nobody else
# (btclib-org/.github#35)

on:
  push:
    # main only, for the reason lint.yml gives at the same key: a push to a
    # branch with an open pull request would run this twice, in two
    # concurrency groups that do not cancel each other for every
    # pull_request action except closed on a merge -- see that trigger's
    # own comment below -- and the pull_request run is the one worth
    # keeping
    branches: [main]
  pull_request:
    # ready_for_review, so a pull request marked ready gets the run the
    # draft condition in reusable-docs.yml withheld while it was a draft.
    # closed too, though the job below declines the real work on it: a
    # merge or a close is neither a push nor a synchronize, so a run
    # this pull request's own concurrency group is still holding
    # survives either unless the closed event lands in that same group
    # -- which this type is what makes it do, cancel-in-progress below
    # turning the landing into the cancellation. That was meant to be
    # *this* pull request's own group and nothing else's: github.ref for
    # a closed, merged pull request resolves to the base branch's ref
    # rather than refs/pull/N/merge, so grouping by github.ref alone put
    # the closed run in the push trigger's own group instead, cancelling
    # the run building the actual merge commit's documentation one to
    # two seconds after every merge -- observed on this repository's own
    # main. The concurrency block below groups a pull_request run by the
    # pull request's own number instead, which cannot equal any push's
    # github.ref under any action
    types: [opened, reopened, synchronize, ready_for_review, closed]
  # lets the release workflow gate publication on this check too
  workflow_call:
    inputs:
      concurrency-suffix:
        description: >
          Appended to the concurrency group, and the release workflow is
          the only caller that passes one. See the group below.
        type: string
        default: ""
  # the escape hatch: a run on demand for a branch whose pull request is
  # not open yet
  workflow_dispatch:

# least privilege for the GITHUB_TOKEN: everything running in a job
# (actions, dependencies) can read the token, so it must not be able to
# write to the repository. The called job needs no elevation of its own,
# reusable-docs.yml's own header naming the same contents: read
permissions:
  contents: read

# cancel superseded runs on the same branch/PR.
# The group is named literally rather than with github.workflow, which in a
# called workflow is the caller's name: the release workflow calls this one
# alongside test.yml and lint.yml, and with that expression the three would
# share a group and cancel each other.
# github.event.pull_request.number, not github.ref alone, is what a
# pull_request run of any action groups by, so a closed run cannot
# resolve to the same group as a push to main -- the pull_request
# trigger's own comment above has the evidence. A push carries no
# pull_request context, so github.ref is still what groups it, unchanged.
# The suffix is a second, independent problem with github.ref: inside a
# called workflow that context is the caller's ref, so a rehearsal -- a
# workflow_dispatch of release.yml on a branch -- computes
# docs-refs/heads/<branch>, the very group a push to that branch or a
# direct dispatch of this workflow computes. One then kills the other.
# release.yml passes a suffix of its own, which keeps the release path
# out of the branch groups (issue btclib-org/.github#1083)
concurrency:
  group: docs-${{ github.event.pull_request.number || github.ref }}${{ inputs.concurrency-suffix }}
  cancel-in-progress: true

jobs:
  # the checkout, the uv setup, the build and the unresolved-link check
  # are btclib-org/.github's; the draft and closed declines are the called
  # workflow's own, reading github.event.pull_request.draft and
  # github.event.action off this caller's trigger. This tree needs no
  # submodule checked out, so `submodules` is left at its default
  docs:
    uses: btclib-org/.github/.github/workflows/reusable-docs.yml@main
    with:
      # the one thing this tree asks of its own built pages that no
      # sibling asks of its own: automodule skips a data member with no
      # word about it where its attribute docstring is not in the module
      # being documented, which no warning and therefore no -W reports.
      # The reusable job runs this against the build directory it already
      # wrote and already has installed for, once the build above is
      # green, so this script pays for no second build and no second
      # install of the docs group -- `.github/scripts/check_api_page.py`'s
      # own docstring has the rest, and `tests/check_api_page_test.py` is
      # where its logic is proven
      post-build-script: .github/scripts/check_api_page.py
